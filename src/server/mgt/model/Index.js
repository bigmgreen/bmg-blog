/**
 * 首页对象类
 * @type {{}}
 */
module.exports = function Index(header, banner, article, author, types) {
    this.header = header;
    this.banner = banner;
    this.article = article;
    this.author = author;
    this.types = types;
};